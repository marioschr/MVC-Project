@using MVC_Project.Models
@model MVC_Project.Models.pub_info

@{
    ViewBag.Title = "Delete";
}

<hr style="border-color: white" />
<h2>Delete</h2>
<h3>Are you sure you want to delete this?</h3>
<hr style="border-color: white" />

<div>
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.logo)
        </dt>

        <dd style="padding-bottom: 1em">
            @Html.Image(Model.logo)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.pr_info)
        </dt>

        <div class="overflow-auto" style="max-height: 10em; max-width: 40em;">
            @Html.DisplayFor(model => model.pr_info)
        </div>

        <dt style=" padding-top: 1em">
            <h4>
                @Html.DisplayNameFor(model => model.publishers.pub_name)
            </h4>
        </dt>

        <dd>
            @Html.DisplayFor(model => model.publishers.pub_name)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <a class="btn btn-primary" href=@Url.Action("Index") role="button">Back to List</a>
            <input type="submit" value="Delete" class="btn btn-danger" />
        </div>
    }
</div>
